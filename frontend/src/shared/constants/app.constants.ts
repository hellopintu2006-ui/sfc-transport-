import { SITE_CONFIG } from '@/config/site.config';

export const APP_CONSTANTS = {
  ROUTES: SITE_CONFIG.routes,
  SERVICES: SITE_CONFIG.services,
  CONTACT: SITE_CONFIG.contact,
  WORKING_HOURS: SITE_CONFIG.hours,
};
